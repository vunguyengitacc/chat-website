import { makeStyles } from "@mui/styles";

const useSearchBarStyle = makeStyles({
  surface: {
    width: "100%",
    height: "100%",
    overflowY: "scroll",
  },
  formSpace: {
    display: "flex",
  },
});

export default useSearchBarStyle;
