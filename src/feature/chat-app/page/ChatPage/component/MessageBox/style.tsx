import { makeStyles } from "@mui/styles";

const useMessageBoxStyle = makeStyles({
  surface: {
    width: "100%",
    height: "100%",
    display: "flex",
    flexDirection: "column",
  },
});

export default useMessageBoxStyle;
