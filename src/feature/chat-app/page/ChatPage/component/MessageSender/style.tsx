import { makeStyles } from "@mui/styles";

const useMessageSenderStyle = makeStyles({
  surface: {
    width: "100%",
    height: "100%",
    display: "flex",
    alignItems: "center",
  },
  input: {
    height: "100%",
    fontSize: "18px",
  },
  btnSubmit: {},
});

export default useMessageSenderStyle;
