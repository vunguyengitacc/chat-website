import { makeStyles } from "@mui/styles";

const useRoomAvatarStyle = makeStyles({
  surface: {
    width: "100%",
    height: "100%",
  },
  avatar: {
    width: "90%",
    height: "90%",
  },
});

export default useRoomAvatarStyle;
