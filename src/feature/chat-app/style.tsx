import { makeStyles } from "@mui/styles";

const useChatAppStyle = makeStyles({
  surface: {
    backgroundColor: "white",
    width: "100vw",
    height: "100vh",
    display: "flex",
  },
});

export default useChatAppStyle;
